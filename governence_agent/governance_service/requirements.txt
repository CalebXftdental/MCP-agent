# MCP server (Streamable HTTP transport)
mcp>=1.10.0

# ASGI runtime
uvicorn[standard]>=0.34.0
starlette>=0.40.0

# Credentialed HTTP clients (miniERP GraphQL, HubSpot REST)
httpx>=0.27.0
requests

# Env loading
python-dotenv

# Optional: contacts.json/phonebook.json served from Azure Blob instead of
# the local JSON files. Safe to leave installed even if unused -- the load
# path in hubspot_query.py falls back to the local files if this import fails
# or no connection string is configured.
azure-storage-blob
