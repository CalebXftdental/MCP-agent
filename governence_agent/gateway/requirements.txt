# MCP -- server (to callers) AND client (to backends). Pinned exact: an
# unbounded floating version broke this code against the newest mcp release
# (FastMCP import path + streamablehttp_client renamed). See requirements.txt.
mcp==1.28.1

# ASGI runtime
uvicorn[standard]==0.51.0
starlette==1.3.1

# httpx backs the MCP Streamable HTTP client used to reach backends
httpx==0.28.1

# Env loading
python-dotenv==1.2.2

# LLM client for the Stage-1 governed chat orchestrator (Azure OpenAI). Imported
# lazily; the gateway runs without it (chat returns "not configured").
openai==2.46.0

# Backup chat LLM (Claude Sonnet via an Anthropic-compatible endpoint), used
# when USE_LOCAL_LLM=false. Imported lazily.
anthropic==0.120.2
