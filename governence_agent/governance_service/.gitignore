# Local secrets
.env
.env.local
.env.*.local

# Real sales-rep directory (names/phones/emails) -- local-file fallback used
# only when CONTACTS_BLOB_*/PHONEBOOK_BLOB_* env vars aren't set. Never commit
# real data here; see hubspot/*.example.json for the template shape.
hubspot/contacts.json
hubspot/phonebook.json

# Caches
__pycache__/
*.pyc
